import javax.swing.*;

public class EncryptedMessage extends JFrame {
    public  EncryptedMessage() {
        initUI();
    }
    private void initUI() {
        JLabel TODO = new JLabel("TODO by 赵炯阳");
        add(TODO);

        setVisible(true);
        setSize(600,400);
    }
}
